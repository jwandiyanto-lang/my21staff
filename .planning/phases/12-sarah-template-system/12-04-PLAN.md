---
phase: 12-sarah-template-system
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/[workspace]/your-team/your-team-client.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Brain tab does not appear on Your Team page"
    - "Only Intern tab is visible in the tabs list"
    - "BrainSettings component is not imported or rendered"
  artifacts:
    - path: "src/app/(dashboard)/[workspace]/your-team/your-team-client.tsx"
      provides: "Your Team page without Brain tab"
      pattern: "TabsTrigger.*intern"
  key_links: []
---

<objective>
Remove Brain tab from Your Team page

Purpose: User feedback (Test 7) confirms Brain settings still visible on Your Team page. This should be hidden to simplify the interface per Phase 12 decisions.

Output: Your Team page shows only the Intern tab with no Brain configuration visible.
</objective>

<execution_context>
@/home/jfransisco/.claude/get-shit-done/workflows/execute-plan.md
@/home/jfransisco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/(dashboard)/[workspace]/your-team/your-team-client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Brain tab and related code from Your Team page</name>
  <files>src/app/(dashboard)/[workspace]/your-team/your-team-client.tsx</files>
  <action>
1. Remove Brain-related imports:
   - Remove `Brain` from lucide-react import
   - Remove `BrainSettings` import from `@/components/your-team/brain-settings`

2. Remove Brain tab trigger (lines 58-61):
   ```tsx
   <TabsTrigger value="brain" className="flex items-center gap-2 py-3">
     <Brain className="w-4 h-4 shrink-0" />
     <span>Brain</span>
   </TabsTrigger>
   ```

3. Remove entire Brain TabsContent section (lines 91-109):
   ```tsx
   {/* Brain Tab - Grok Manager Bot configuration */}
   <TabsContent value="brain" className="space-y-6">
     ...BrainSettings...
   </TabsContent>
   ```

4. Update TabsList to single column since only one tab:
   - Change `grid-cols-2` to remove grid entirely OR keep single visible tab
   - Consider: If only one tab, the TabsList may not be needed at all. Use simpler header instead.

5. Simplify to single-tab layout:
   - Remove Tabs wrapper entirely since there's only one "tab"
   - Keep the Intern content inline without Tab structure
   - This provides cleaner UX when only one option exists

Result: Page shows Intern content directly without tab navigation.
  </action>
  <verify>
1. Run `npm run lint` - no errors
2. Run `npm run type-check` - no TypeScript errors
3. Grep for "Brain" in the file: `grep -n "Brain" src/app/(dashboard)/[workspace]/your-team/your-team-client.tsx` - should return empty
4. Grep for "brain" in the file: `grep -n "brain" src/app/(dashboard)/[workspace]/your-team/your-team-client.tsx` - should return empty
  </verify>
  <done>
- Brain tab trigger removed from TabsList
- Brain TabsContent section removed
- BrainSettings component import removed
- Brain icon import removed
- No TypeScript or lint errors
- File compiles successfully
  </done>
</task>

</tasks>

<verification>
1. `npm run lint` passes
2. `npm run type-check` passes
3. `grep -rn "BrainSettings" src/app/(dashboard)/[workspace]/your-team/` returns no matches
4. `grep -rn "value=\"brain\"" src/app/(dashboard)/[workspace]/your-team/` returns no matches
</verification>

<success_criteria>
- Brain tab is completely removed from Your Team page
- No Brain-related imports remain in the file
- Page shows only Intern content
- No TypeScript or lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-sarah-template-system/12-04-SUMMARY.md`
</output>
