---
phase: 07-landing-page-redesign
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/landing/hero-section.tsx
  - src/components/landing/features-grid.tsx
  - src/components/landing/feature-card.tsx
  - src/components/landing/index.ts
autonomous: true

must_haves:
  truths:
    - "Hero section displays '24/7 Digital Workforce' headline"
    - "Hero section has WhatsApp CTA visible above the fold"
    - "Features grid displays 4 feature cards in minimalist style"
    - "All components are mobile-first responsive"
  artifacts:
    - path: "src/components/landing/hero-section.tsx"
      provides: "Hero with headline, supporting text, CTA, StaffDeck"
      contains: "24/7 Digital Workforce"
    - path: "src/components/landing/features-grid.tsx"
      provides: "Grid of feature cards"
      exports: ["FeaturesGrid"]
    - path: "src/components/landing/feature-card.tsx"
      provides: "Individual feature card component"
      exports: ["FeatureCard"]
  key_links:
    - from: "src/components/landing/hero-section.tsx"
      to: "src/lib/landing-constants.ts"
      via: "import WHATSAPP_LINK"
      pattern: "WHATSAPP_LINK"
    - from: "src/components/landing/features-grid.tsx"
      to: "src/lib/landing-constants.ts"
      via: "import FEATURES"
      pattern: "FEATURES"
---

<objective>
Build hero section and features grid for the English landing page redesign.

Purpose: Create the main content sections with mobile-first design and minimalist aesthetic per CONTEXT.md.
Output: HeroSection, FeaturesGrid, FeatureCard components ready for page integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-landing-page-redesign/07-CONTEXT.md
@.planning/phases/07-landing-page-redesign/07-RESEARCH.md
@.planning/phases/07-landing-page-redesign/07-01-SUMMARY.md
@src/app/globals.css (landing tokens: landing-bg, landing-hero, landing-cta, landing-text)
@src/components/ui/staff-deck.tsx (reuse in hero)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HeroSection component</name>
  <files>src/components/landing/hero-section.tsx</files>
  <action>
Create mobile-first hero section with:

**Layout (CONTEXT.md requirements):**
- Headline: "24/7 Digital Workforce" (Plus Jakarta Sans, font-extrabold)
- Supporting text: WhatsApp automation message + CRM grows with you
- Primary CTA: WhatsApp click-to-chat button (orange, rounded-full)
- Visual: Reuse existing StaffDeck component (import from @/components/ui/staff-deck)

**Mobile-first approach (RESEARCH.md patterns):**
- Mobile: Single column, text stacked, StaffDeck below text
- Desktop (md+): Two-column grid, text left, StaffDeck right
- Hero CTA must be visible above the fold (no scroll required)

**Styling per CONTEXT.md:**
- Background: landing-hero (#B6C9BB sage green)
- Text: white on sage background
- CTA: landing-cta (#F7931A orange)
- Min-height: 80vh on mobile, full screen on desktop

**Animation (RESEARCH.md - only transform/opacity):**
- Use Framer Motion for subtle fade-in-up on text
- Keep animations minimal per "rams.ai minimalist aesthetic"
- Use 'use client' directive for Framer Motion

**Import from constants:**
- WHATSAPP_LINK from @/lib/landing-constants

**WhatsApp CTA structure:**
```tsx
<Link
  href={WHATSAPP_LINK}
  target="_blank"
  rel="noopener"
  className="inline-flex items-center gap-2 px-8 py-4 bg-landing-cta text-white font-bold rounded-full hover:bg-landing-cta-dark transition-colors"
>
  <MessageCircle className="w-5 h-5" />
  Chat with Us
</Link>
```
  </action>
  <verify>
Component renders without errors:
```bash
npx tsc src/components/landing/hero-section.tsx --noEmit
```
  </verify>
  <done>HeroSection component created with "24/7 Digital Workforce" headline, WhatsApp CTA, and StaffDeck</done>
</task>

<task type="auto">
  <name>Task 2: Create FeatureCard component</name>
  <files>src/components/landing/feature-card.tsx</files>
  <action>
Create minimalist feature card component:

**Props interface:**
```typescript
interface FeatureCardProps {
  icon: string  // Lucide icon name
  title: string
  description: string
}
```

**Styling per CONTEXT.md (minimalist cards):**
- White background with subtle border (border-gray-100)
- Rounded corners (rounded-2xl)
- Icon in colored circle (landing-cta/10 background)
- Clean typography: title bold (font-bold), description muted (text-landing-text-muted)
- Hover state: subtle border color change (border-landing-cta/30)

**Icon resolution:**
- Use dynamic Lucide icon import pattern:
```tsx
import * as LucideIcons from 'lucide-react'

const IconComponent = LucideIcons[icon as keyof typeof LucideIcons] as LucideIcons.LucideIcon
```

**Mobile-first padding:**
- p-6 base padding
- No complex hover animations (keep it minimalist)

This is a server component (no 'use client' needed - no state/effects).
  </action>
  <verify>
Component renders without errors:
```bash
npx tsc src/components/landing/feature-card.tsx --noEmit
```
  </verify>
  <done>FeatureCard component created with dynamic icon, minimalist styling</done>
</task>

<task type="auto">
  <name>Task 3: Create FeaturesGrid component and update barrel export</name>
  <files>src/components/landing/features-grid.tsx, src/components/landing/index.ts</files>
  <action>
**FeaturesGrid component:**

Create grid component that renders feature cards from FEATURES constant:

**Structure:**
- Section with white/off-white background (bg-white or bg-landing-bg)
- Section title: "What We Do" or similar (English, minimal)
- Grid of FeatureCard components

**Mobile-first grid:**
- Mobile: 1 column (grid-cols-1)
- Tablet: 2 columns (md:grid-cols-2)
- Desktop: 4 columns or 2x2 (lg:grid-cols-4 or lg:grid-cols-2)
- Gap: gap-4 mobile, md:gap-6 desktop

**Import:**
- FEATURES from @/lib/landing-constants
- FeatureCard from ./feature-card

**Animation (optional, minimal):**
- Can add Framer Motion whileInView for stagger effect
- If adding animation, use 'use client' directive
- Only animate opacity and y (transform)

**Update index.ts barrel:**
Add exports for all three components:
```typescript
export { HeroSection } from './hero-section'
export { FeatureCard } from './feature-card'
export { FeaturesGrid } from './features-grid'
```
  </action>
  <verify>
All components compile:
```bash
npx tsc src/components/landing/*.tsx --noEmit
```
  </verify>
  <done>FeaturesGrid renders 4 feature cards in responsive grid, index.ts exports all components</done>
</task>

</tasks>

<verification>
- [ ] HeroSection displays "24/7 Digital Workforce" headline
- [ ] HeroSection has WhatsApp CTA button using WHATSAPP_LINK
- [ ] HeroSection integrates StaffDeck component
- [ ] HeroSection is mobile-first (single column mobile, two-column desktop)
- [ ] FeatureCard displays icon, title, description with minimalist styling
- [ ] FeaturesGrid renders 4 cards from FEATURES constant
- [ ] FeaturesGrid is responsive (1 col mobile, 2+ cols desktop)
- [ ] All components export from index.ts
- [ ] TypeScript compiles without errors
</verification>

<success_criteria>
- Hero section complete with headline, CTA, and visual
- Features grid displays all features in minimalist cards
- Mobile-first responsive design implemented
- Components ready for page integration in Plan 03
</success_criteria>

<output>
After completion, create `.planning/phases/07-landing-page-redesign/07-02-SUMMARY.md`
</output>
