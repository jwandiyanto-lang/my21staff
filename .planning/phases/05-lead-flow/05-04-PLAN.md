---
phase: 05-lead-flow
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - src/components/workspace/sidebar.tsx
  - src/app/(dashboard)/[workspace]/database/database-client.tsx
autonomous: true

must_haves:
  truths:
    - "Sidebar shows 'Database' instead of 'Lead Management'"
    - "Database page header shows 'Database' instead of 'Lead Management'"
  artifacts:
    - path: "src/components/workspace/sidebar.tsx"
      provides: "Navigation link to Database"
      contains: "title: 'Database'"
    - path: "src/app/(dashboard)/[workspace]/database/database-client.tsx"
      provides: "Database page header"
      contains: "<h1 className=\"text-2xl font-semibold\">Database</h1>"
  key_links:
    - from: "sidebar.tsx"
      to: "/database route"
      via: "operationsNav array"
      pattern: "title.*Database"
---

<objective>
Rename "Lead Management" to "Database" throughout the UI

Purpose: Align navigation and page titles with the intended naming convention
Output: Sidebar nav and page header both display "Database"
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@planning/PROJECT.md
@planning/ROADMAP.md
@src/components/workspace/sidebar.tsx
@src/app/(dashboard)/[workspace]/database/database-client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename sidebar navigation item</name>
  <files>src/components/workspace/sidebar.tsx</files>
  <action>
In the operationsNav array (line 28-44), change the title for the database route:

Before:
```typescript
{
  title: 'Lead Management',
  icon: Users,
  href: '/database',
},
```

After:
```typescript
{
  title: 'Database',
  icon: Users,
  href: '/database',
},
```

This is on line 40 of the file.
  </action>
  <verify>
Read the file and confirm `title: 'Database'` appears in operationsNav array
  </verify>
  <done>
Sidebar navigation shows "Database" instead of "Lead Management"
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename page header</name>
  <files>src/app/(dashboard)/[workspace]/database/database-client.tsx</files>
  <action>
In the DatabaseClient component, find the h1 header (around line 312):

Before:
```tsx
<h1 className="text-2xl font-semibold">Lead Management</h1>
```

After:
```tsx
<h1 className="text-2xl font-semibold">Database</h1>
```
  </action>
  <verify>
Read the file and confirm the h1 element contains "Database"
  </verify>
  <done>
Database page header shows "Database" instead of "Lead Management"
  </done>
</task>

</tasks>

<verification>
- [ ] Sidebar nav item shows "Database"
- [ ] Page header shows "Database"
- [ ] No TypeScript errors: `npx tsc --noEmit`
- [ ] App loads without errors: `npm run dev`
</verification>

<success_criteria>
UI naming is consistent:
1. Sidebar shows "Database" in the Operations section
2. Database page shows "Database" as the h1 header
3. No build or runtime errors
</success_criteria>

<output>
After completion, create `planning/phases/05-lead-flow/05-04-SUMMARY.md`
</output>
