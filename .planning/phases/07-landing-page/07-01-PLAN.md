---
phase: 07-landing-page
plan: 01
type: execute
---

<objective>
Set up landing page design system and create hero section.

Purpose: Establish the visual foundation for the public landing page with Notion-inspired design tokens and a compelling hero that communicates the "Digital Staff" concept.
Output: Landing page color tokens in globals.css, hero section with navigation at `/`.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-landing-page/07-CONTEXT.md
@src/app/globals.css
@src/app/page.tsx
@src/app/layout.tsx

**Tech stack available:** Next.js 15, React 19, Tailwind CSS v4, Shadcn/ui
**Established patterns:** oklch color tokens, CSS variables theming

**Design specs from CONTEXT:**
- Background: #FBFBFA (off-white)
- Primary CTA: #F7931A (orange)
- Hero background: #B6C9BB (sage green)
- Text: #37352F (primary), #37352F99 (muted)
- Typography: Plus Jakarta Sans (display), Inter (body), JetBrains Mono (labels)
- Buttons: rounded-notion (3px), uppercase tracking-widest
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add landing page design tokens to globals.css</name>
  <files>src/app/globals.css</files>
  <action>Add a new CSS section for landing page tokens after the existing CRM tokens. Use oklch color values converted from the hex specs:

Landing page tokens to add:
- --landing-bg: #FBFBFA (off-white)
- --landing-hero: #B6C9BB (sage green)
- --landing-cta: #F7931A (orange - same as existing primary)
- --landing-text: #37352F (near-black)
- --landing-text-muted: #37352F99 (60% opacity)
- --landing-dark-bg: #1A1A1A (for CTA section)
- --landing-border: 1px solid rgba(55, 53, 47, 0.16) (notion-style border)

Also add utility classes:
- .rounded-notion { border-radius: 3px }
- .border-notion { border: 1px solid rgba(55, 53, 47, 0.16) }
- .border-notion-thick { border: 2px solid rgba(55, 53, 47, 0.16) }
- .notion-grid (subtle grid pattern for hero background)

Do NOT modify existing CRM tokens - add these as separate landing page tokens.</action>
  <verify>globals.css has landing-* tokens, `npm run build` passes without errors</verify>
  <done>Landing page color tokens and utility classes added to globals.css</done>
</task>

<task type="auto">
  <name>Task 2: Create landing page with hero section</name>
  <files>src/app/page.tsx</files>
  <action>Replace the default Next.js starter page with the landing page hero section:

Structure:
1. Navigation bar (fixed top): Logo "21" on left, "Login" link on right pointing to /auth/login
2. Hero section with sage green background (#B6C9BB / var(--landing-hero)):
   - Main heading: "Your 21 Digital Staff" (Plus Jakarta Sans, bold)
   - Subheading: "A full digital department for the price of one entry-level employee"
   - Orange CTA button: "Get Started" (rounded-notion, uppercase, tracking-widest)
   - Subtle notion-grid pattern overlay on hero background

Typography:
- Import Plus Jakarta Sans via next/font/google (variable --font-jakarta)
- Hero heading: text-5xl md:text-6xl font-bold
- Subheading: text-xl text-landing-text-muted
- Button: bg-landing-cta text-white uppercase tracking-widest px-8 py-3

Keep it simple - no animations (out of scope). Desktop-first layout.</action>
  <verify>`npm run dev` shows landing page at localhost:3000 with sage green hero, orange button</verify>
  <done>Landing page hero section renders with correct colors, typography, and navigation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Landing page loads at `/` with sage green hero
- [ ] Orange CTA button visible with correct styling
- [ ] Navigation shows logo and login link
- [ ] No TypeScript errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Landing page design tokens established
- Hero section matches design specs
</success_criteria>

<output>
After completion, create `.planning/phases/07-landing-page/07-01-SUMMARY.md`
</output>
