---
phase: 08-performance-optimization
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/(dashboard)/[workspace]/loading.tsx
  - src/app/(dashboard)/[workspace]/inbox/loading.tsx
  - src/app/(dashboard)/[workspace]/database/loading.tsx
  - src/app/(dashboard)/[workspace]/support/loading.tsx
  - src/app/(dashboard)/[workspace]/settings/loading.tsx
  - src/components/skeletons/dashboard-skeleton.tsx
  - src/components/skeletons/inbox-skeleton.tsx
  - src/components/skeletons/table-skeleton.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard route shows skeleton during SSR data fetch"
    - "Inbox route shows skeleton during initial load"
    - "Database route shows table skeleton during load"
    - "All skeletons use consistent visual pattern (Skeleton component)"
  artifacts:
    - path: "src/app/(dashboard)/[workspace]/loading.tsx"
      provides: "Dashboard loading skeleton"
      contains: "DashboardSkeleton"
    - path: "src/app/(dashboard)/[workspace]/inbox/loading.tsx"
      provides: "Inbox loading skeleton"
      contains: "InboxSkeleton"
    - path: "src/components/skeletons/dashboard-skeleton.tsx"
      provides: "Reusable dashboard skeleton component"
      exports: ["DashboardSkeleton"]
  key_links:
    - from: "src/app/(dashboard)/[workspace]/loading.tsx"
      to: "src/components/skeletons/dashboard-skeleton.tsx"
      via: "import DashboardSkeleton"
      pattern: "DashboardSkeleton"
---

<objective>
Create consistent loading states using Next.js loading.tsx convention and reusable skeleton components.

Purpose: Eliminate blank/white screens during navigation and data loading. Users see content placeholders immediately.
Output: loading.tsx files for key routes, reusable skeleton components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-performance-optimization/08-RESEARCH.md
@.planning/phases/08-performance-optimization/08-01-SUMMARY.md
@src/components/ui/skeleton.tsx (existing Skeleton component)
@src/app/(dashboard)/[workspace]/page.tsx (dashboard structure reference)

Current state:
- No loading.tsx files exist in dashboard routes
- Skeleton component exists but is underutilized
- Pages show blank/white screen until data loads
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skeleton components for common patterns</name>
  <files>
    - src/components/skeletons/dashboard-skeleton.tsx
    - src/components/skeletons/inbox-skeleton.tsx
    - src/components/skeletons/table-skeleton.tsx
  </files>
  <action>
Create src/components/skeletons/dashboard-skeleton.tsx:
```typescript
import { Card, CardContent, CardHeader } from '@/components/ui/card'
import { Skeleton } from '@/components/ui/skeleton'

export function DashboardSkeleton() {
  return (
    <div className="p-6 space-y-6">
      {/* Header */}
      <div>
        <Skeleton className="h-8 w-48" />
        <Skeleton className="h-4 w-32 mt-2" />
      </div>

      {/* Stats Grid */}
      <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4">
        {Array.from({ length: 4 }).map((_, i) => (
          <Card key={i}>
            <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
              <Skeleton className="h-4 w-24" />
              <Skeleton className="h-4 w-4" />
            </CardHeader>
            <CardContent>
              <Skeleton className="h-8 w-16" />
              <Skeleton className="h-3 w-20 mt-2" />
            </CardContent>
          </Card>
        ))}
      </div>

      {/* Secondary Stats Section */}
      <div>
        <Skeleton className="h-6 w-40 mb-4" />
        <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4">
          {Array.from({ length: 4 }).map((_, i) => (
            <Card key={i} className="border-orange-200 bg-orange-50/50">
              <CardHeader className="pb-2">
                <Skeleton className="h-4 w-20 bg-orange-200" />
              </CardHeader>
              <CardContent>
                <Skeleton className="h-8 w-12 bg-orange-200" />
              </CardContent>
            </Card>
          ))}
        </div>
      </div>

      {/* Tasks Section */}
      <div>
        <Skeleton className="h-6 w-36 mb-4" />
        <Card>
          <CardContent className="p-4">
            {Array.from({ length: 3 }).map((_, i) => (
              <div key={i} className="flex items-center gap-3 py-3 border-b last:border-0">
                <Skeleton className="h-4 w-4" />
                <div className="flex-1">
                  <Skeleton className="h-4 w-3/4" />
                  <Skeleton className="h-3 w-1/2 mt-1" />
                </div>
                <Skeleton className="h-6 w-16" />
              </div>
            ))}
          </CardContent>
        </Card>
      </div>
    </div>
  )
}
```

Create src/components/skeletons/inbox-skeleton.tsx:
```typescript
import { Skeleton } from '@/components/ui/skeleton'

export function InboxSkeleton() {
  return (
    <div className="flex h-full">
      {/* Left sidebar - Conversation list */}
      <div className="w-80 border-r bg-background flex flex-col">
        {/* Search */}
        <div className="p-4 border-b space-y-3">
          <Skeleton className="h-10 w-full" />
          <div className="flex gap-2">
            <Skeleton className="h-8 w-20 rounded-full" />
            <Skeleton className="h-8 w-24 rounded-full" />
            <Skeleton className="h-8 w-16 rounded-full" />
          </div>
        </div>

        {/* Conversation items */}
        <div className="flex-1 p-2 space-y-1">
          {Array.from({ length: 8 }).map((_, i) => (
            <div key={i} className="p-3 rounded-lg">
              <div className="flex items-center gap-3">
                <Skeleton className="h-10 w-10 rounded-full" />
                <div className="flex-1">
                  <Skeleton className="h-4 w-32" />
                  <Skeleton className="h-3 w-48 mt-1" />
                </div>
                <Skeleton className="h-3 w-10" />
              </div>
            </div>
          ))}
        </div>
      </div>

      {/* Right area - Message thread placeholder */}
      <div className="flex-1 flex flex-col bg-muted/30">
        {/* Header */}
        <div className="p-4 border-b bg-background flex items-center gap-3">
          <Skeleton className="h-10 w-10 rounded-full" />
          <div>
            <Skeleton className="h-5 w-32" />
            <Skeleton className="h-3 w-24 mt-1" />
          </div>
        </div>

        {/* Messages area */}
        <div className="flex-1 p-4 space-y-4">
          {Array.from({ length: 5 }).map((_, i) => (
            <div key={i} className={`flex ${i % 2 === 0 ? 'justify-start' : 'justify-end'}`}>
              <Skeleton className={`h-16 ${i % 2 === 0 ? 'w-64' : 'w-48'} rounded-lg`} />
            </div>
          ))}
        </div>

        {/* Input area */}
        <div className="p-4 border-t bg-background">
          <Skeleton className="h-20 w-full rounded-lg" />
        </div>
      </div>
    </div>
  )
}
```

Create src/components/skeletons/table-skeleton.tsx:
```typescript
import { Skeleton } from '@/components/ui/skeleton'

interface TableSkeletonProps {
  columns?: number
  rows?: number
}

export function TableSkeleton({ columns = 6, rows = 10 }: TableSkeletonProps) {
  return (
    <div className="p-6 space-y-6">
      {/* Header */}
      <div className="flex items-center justify-between">
        <div>
          <Skeleton className="h-8 w-48" />
          <Skeleton className="h-4 w-32 mt-2" />
        </div>
      </div>

      {/* Filters */}
      <div className="flex items-center gap-3">
        <Skeleton className="h-9 w-32" />
        <Skeleton className="h-9 w-24" />
        <Skeleton className="h-9 w-28" />
        <Skeleton className="h-9 w-24" />
      </div>

      {/* Table */}
      <div className="border rounded-lg">
        {/* Header row */}
        <div className="border-b p-4 flex gap-4">
          {Array.from({ length: columns }).map((_, i) => (
            <Skeleton key={i} className="h-4 flex-1" />
          ))}
        </div>

        {/* Data rows */}
        {Array.from({ length: rows }).map((_, rowIndex) => (
          <div key={rowIndex} className="border-b last:border-0 p-4 flex gap-4 items-center">
            {Array.from({ length: columns }).map((_, colIndex) => (
              <Skeleton
                key={colIndex}
                className={`h-4 flex-1 ${colIndex === 0 ? 'w-48' : ''}`}
              />
            ))}
          </div>
        ))}
      </div>

      {/* Pagination */}
      <div className="flex items-center justify-center gap-2">
        <Skeleton className="h-8 w-8" />
        <Skeleton className="h-8 w-8" />
        <Skeleton className="h-8 w-8" />
        <Skeleton className="h-8 w-8" />
        <Skeleton className="h-8 w-8" />
      </div>
    </div>
  )
}
```
  </action>
  <verify>
- Files created without TypeScript errors
- Import Skeleton from correct path
- Components render without errors (test by importing in a page temporarily)
  </verify>
  <done>Three reusable skeleton components created matching actual page layouts</done>
</task>

<task type="auto">
  <name>Task 2: Create loading.tsx files for dashboard routes</name>
  <files>
    - src/app/(dashboard)/[workspace]/loading.tsx
    - src/app/(dashboard)/[workspace]/inbox/loading.tsx
    - src/app/(dashboard)/[workspace]/database/loading.tsx
    - src/app/(dashboard)/[workspace]/support/loading.tsx
    - src/app/(dashboard)/[workspace]/settings/loading.tsx
  </files>
  <action>
Create src/app/(dashboard)/[workspace]/loading.tsx:
```typescript
import { DashboardSkeleton } from '@/components/skeletons/dashboard-skeleton'

export default function Loading() {
  return <DashboardSkeleton />
}
```

Create src/app/(dashboard)/[workspace]/inbox/loading.tsx:
```typescript
import { InboxSkeleton } from '@/components/skeletons/inbox-skeleton'

export default function Loading() {
  return <InboxSkeleton />
}
```

Create src/app/(dashboard)/[workspace]/database/loading.tsx:
```typescript
import { TableSkeleton } from '@/components/skeletons/table-skeleton'

export default function Loading() {
  return <TableSkeleton columns={7} rows={10} />
}
```

Create src/app/(dashboard)/[workspace]/support/loading.tsx:
```typescript
import { TableSkeleton } from '@/components/skeletons/table-skeleton'

export default function Loading() {
  return <TableSkeleton columns={7} rows={8} />
}
```

Create src/app/(dashboard)/[workspace]/settings/loading.tsx:
```typescript
import { Skeleton } from '@/components/ui/skeleton'
import { Card, CardContent, CardHeader } from '@/components/ui/card'

export default function Loading() {
  return (
    <div className="p-6 space-y-6">
      {/* Header */}
      <div>
        <Skeleton className="h-8 w-32" />
        <Skeleton className="h-4 w-48 mt-2" />
      </div>

      {/* Settings Cards */}
      <div className="grid gap-6 md:grid-cols-2">
        {Array.from({ length: 4 }).map((_, i) => (
          <Card key={i}>
            <CardHeader>
              <Skeleton className="h-5 w-40" />
              <Skeleton className="h-4 w-64 mt-1" />
            </CardHeader>
            <CardContent className="space-y-4">
              <Skeleton className="h-10 w-full" />
              <Skeleton className="h-10 w-full" />
              <Skeleton className="h-9 w-24" />
            </CardContent>
          </Card>
        ))}
      </div>
    </div>
  )
}
```

These loading.tsx files are automatically used by Next.js when the page.tsx is loading (during SSR data fetch or suspense).
  </action>
  <verify>
- Navigate to dashboard - skeleton shows briefly during load
- Navigate to inbox - inbox skeleton shows
- Navigate to database - table skeleton shows
- Navigate to support - table skeleton shows
- Hard refresh on each page shows skeleton before content
  </verify>
  <done>loading.tsx files create smooth loading experience for all main dashboard routes</done>
</task>

</tasks>

<verification>
1. Hard refresh dashboard - skeleton visible before content
2. Hard refresh inbox - conversation list skeleton visible
3. Hard refresh database - table skeleton visible
4. Navigate between routes - appropriate skeletons show
5. No layout shift when content loads (skeletons match actual layout)
6. All skeletons use consistent styling (Skeleton component with animate-pulse)
</verification>

<success_criteria>
- No blank screens during navigation
- Skeletons match actual content layout (no jarring shift)
- loading.tsx files in all main dashboard routes
- Reusable skeleton components for common patterns
- Consistent visual language across all loading states
</success_criteria>

<output>
After completion, create `.planning/phases/08-performance-optimization/08-03-SUMMARY.md`
</output>
